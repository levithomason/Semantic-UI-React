import { rem } from '../../lib'

export default () => ({
  root: {
    display: 'flex',
    border: '1px solid rgba(34, 36, 38, .15)',
    borderRadius: rem(0.28571429),
    minHeight: rem(2.85714286),
  },
})
